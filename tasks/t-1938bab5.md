---
id: t-1938bab5
title: Hermes v0.17 — Tier 1: fix 5 pre-existing CLI/allowlist bugs + surface config-set failures
status: done
added: 2026-06-21
priority: high
---

## Description

Pre-existing bugs surfaced by the Hermes v0.17 audit (broken on v0.16 AND v0.17; live-confirmed against the binary). See [[Hermes v0.17.0 Audit Findings]]. Independent of v0.17 — these hurt current users now. GATED: another session (t-7b47ba93, fix/remote-sync-io-on-main) is editing HermesFileService/Chat; wait for it to land, then cut feat/hermes-v017-parity from the clean state and re-ground line numbers.

1. HealthViewModel.swift:571 — `["audit"]` is not a verb → routes to chat agent (burns a turn). Use `["security","audit"]`; re-verify the audit-output parser still matches.
2. HealthViewModel.swift:623 — `["migrate","xai"]` is dry-run by default → never migrates but reports success. Add `--apply`; confirm the retired-model warning clears after.
3. HealthView.swift:181 — `--assume-yes` is invalid (argparse exit 2). Use `--yes`.
4. CuratorService.swift:113-119 — appends nonexistent `--json` to `curator prune`; dry-run preview shows archived not candidates; destructive path lacks `-y` → hangs to timeout. Drop `--json`, parse the text candidate rows (`curator: N skill(s) idle >= Nd:`), add `-y` on destructive. (Relates to existing t-todo-ws4.)
5. WhatsApp allowlist (GatewayAllowlistKind.swift:71) — writes `whatsapp.allowed_chats`, which Hermes never reads (uses `allow_from`/`group_allow_from` + `dm_policy: allowlist`) → silent no-op. RECOMMEND folding the proper allow_from modeling into the Tier 2 gateway phase (alongside whatsapp_cloud); minimal interim = drop WhatsApp from the allowlist editor so it stops lying. (Relates to t-todo-ws5.)
6. Managed-scope cheap win: SettingsViewModel.setSetting (and sibling write sites incl. HermesFileService model writes) only log on non-zero `config set` exit. Surface the failure/stderr to the user — improves general error UX and is the minimum safe handling for the new managed-scope (/etc/hermes) write-rejection.

VERIFY: unit tests where parsers change (curator prune text parse); manual check on a real host that the audit button produces a report, migrate xai actually rewrites, browser setup auto-confirms.

## Plan



## Artifacts



